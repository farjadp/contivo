{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pnpm install --no-frozen-lockfile --prod=false --config.node-linker=isolated && pnpm --filter @contivo/types build && pnpm --filter @contivo/web build"
  },
  "deploy": {
    "startCommand": "pnpm --filter @contivo/web start",
    "healthcheckPath": "/api/health",
    "healthcheckTimeout": 120,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
